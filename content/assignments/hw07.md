---
title: "Building Maintainable Wireframes in Figma"
type: "homework"
num: "7"
draft: 1
heading_max_level: 3
assigned_date: "2026-03-19"
due_date: "2026-03-24"
---

> ## Overview
> 
> In this assignment, you will create a small interface using design tokens, components, and responsive layouts in Figma. Specifically, you will:
> 
> 1. Import a design token file
> 1. Learn how tokens control the look of an interface
> 1. Build reusable components
> 1. Design three screens
> 1. Create desktop and mobile layouts
> 
> The goal is to help you understand how modern design systems work and how they support consistent, scalable interfaces.


{% collapsible %}
## 1. Import the Design Tokens

Download and import the provided token pack:

- **figma-multimode-token-pack-with-fonts.zip** (see [HW7 downloads](/spring2026/downloads/hw07/) or your instructor for the link)

In Figma:

1. Open the **Variables** panel
2. Create a new variable collection
3. Import the provided JSON files

Figma will create **multiple modes** (themes). These modes allow the interface to switch between themes such as:

- light-color
- dark-color
- light-grayscale
- dark-grayscale

### 1.1. What Are Design Tokens?

Design tokens are **named values** that control visual properties of the interface.

Instead of manually choosing colors or font sizes everywhere, we use tokens.

| Token | Meaning |
|-------|---------|
| `bg.default` | Default background color |
| `text.primary` | Main body text color |
| `button.primary.bg` | Background color of a primary button |
| `font.size.md` | Standard text size |

Using tokens ensures that the interface stays consistent and can easily support themes (like dark mode).

**Example:** Instead of writing **Fill: #3C5E96**, you use **Fill: button.primary.bg**. If the theme changes, the color updates automatically.


{% collapsible %}
## 2. Understanding Frames

In Figma, **frames** are containers that hold interface elements.

You can think of a frame as similar to:

- a webpage
- a mobile screen
- a div container

Frames can contain:

- text
- images
- buttons
- other frames

For this assignment, each screen should be built **inside a frame**.

**Suggested sizes:**

- **Desktop:** 1440 × 1024
- **Mobile:** 390 × 844


{% collapsible %}
## 3. Auto Layout (Like Flexbox)

Figma’s **Auto Layout** feature works very similarly to CSS Flexbox.

It allows elements to:

- align automatically
- maintain spacing
- adapt to different screen sizes

For example, Auto Layout can:

- stack items vertically
- distribute items horizontally
- maintain consistent spacing between elements

This makes responsive design much easier.

**Recommended:** Watch a short video on Auto Layout and how it works like Flexbox (your instructor may provide a link).


{% collapsible %}
## 4. Components

A **component** is a reusable UI element.

Instead of redesigning the same thing many times, you design it once and reuse it.

Examples of components include:

- buttons
- navigation bars
- cards
- input fields

If you create a button component, you can reuse it on many screens. If you update the main component, all copies update automatically.

### 4.1. Variants

Components can have **variants**, which represent different versions of the same element.

**Example: Button component**

Variants might include:

- Primary
- Secondary
- Ghost
- Disabled

All of these belong to the same component, but have slightly different styling. This helps keep designs consistent.


{% collapsible %}
## 5. Build Three Components

Create the following reusable components:

**Button**

- Include variants for: **Primary**, **Secondary**, **Ghost**
- Use the design tokens for: colors, font styles, spacing

**Input Field**

- Include: Label, Input field, Helper text, Error state

**Card**

- Contains: Title, Body text, Optional button


{% collapsible %}
## 6. Design Three Screens

Create three screens for a simple application.

You may choose your own theme (task manager, wellness app, activity tracker, etc.).

Each screen should exist in **both desktop and mobile** layouts.

### 6.1. Screen 1 — Dashboard

Include:

- greeting or page title
- list of activities or tasks
- progress indicator
- primary action button

### 6.2. Screen 2 — Activity Detail

Include:

- title
- description
- progress indicator
- action button
- notes or additional information

### 6.3. Screen 3 — Profile / Settings

Include:

- user profile card
- settings or preferences
- theme toggle
- logout button


{% collapsible %}
## 7. Theme Demonstration

Use the **variable modes** from the token file to switch themes.

Demonstrate **at least two themes**.

Example: **light-color** and **dark-color**

Duplicate your screen frame and apply different variable modes to show the same layout under different themes.


{% collapsible %}
## 8. File Organization

Organize your file like this:

**Page: Design System**

- Tokens Preview
- Typography Styles
- Components

**Page: Screens**

- Dashboard – Desktop | Dashboard – Mobile
- Activity Detail – Desktop | Activity Detail – Mobile
- Profile – Desktop | Profile – Mobile


{% collapsible %}
## 9. What You Should Learn

By the end of this assignment, you should understand:

- how design tokens control interface styling
- how components make design reusable
- how variants represent different states
- how Auto Layout supports responsive design
- how themes like dark mode can be implemented using tokens

These concepts are widely used in modern design systems used by companies like Google, Apple, and Shopify.


{% collapsible %}
## What to Submit

Submit according to your instructor’s directions (e.g. Canvas, Figma link, or course site):

- [ ] **Figma file** with:
  - [ ] Design System page (tokens preview, typography styles, Button / Input / Card components)
  - [ ] Screens page (3 screens × desktop + mobile = 6 frames)
  - [ ] Theme demonstration (at least two variable modes applied to your screens)
- [ ] **Link** to view or copy the file (set sharing as needed so your instructor can open it)
