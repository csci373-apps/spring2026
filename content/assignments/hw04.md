---
title: "Frontend Integration"
date: "2026-02-12"
type: "assignment"
num: "4"
draft: 0
due_date: "2026-02-24"
---

You will implement **Module CRUD in the web UI**, similar to the reference implementation in the instructor’s `health-app` UI.

> ## Suggestions for division of labor (teams of 4–5)
>
> You can parallelize this work if you agree on shared types + helper functions early.
> Start by creating GitHub issues that match the “Person X checklist” items in **Section 5**, then have each person work from their issues.
>
> 1. **Person 1:** Shared types + API helpers + route scaffolding
>    - Define `Module`, `ModuleCreate`, and `ModuleUpdate` types (in one shared place)
>    - Implement module API helper functions (one file) and keep endpoint changes localized
>    - Add the create/detail routes in `tma-starter-app/ui/src/App.tsx` so others can build pages against them
> 2. **Person 2:** Course detail “Modules” section (Read/List + navigation)
>    - Update the course detail page to display modules and link to module detail
>    - Add a “Create Module” button/link
> 3. **Person 3:** Create Module page (Create)
>    - Build the create form and wire it to the create endpoint
>    - Handle loading + error states and navigation after success
> 4. **Person 4:** Module detail + Edit (Read one + Update)
>    - Build the module detail page and an edit modal/page
>    - Wire edit to the update endpoint and refresh on success
> 5. **Person 5 (optional):** Delete + testing + polish
>    - Add delete with confirmation + good error messages
>    - Add the minimal Vitest tests for a pure helper
>
> Coordination tip: merge shared types + API helpers first so everyone else can call the same functions.

## 1. Assigned reading

1. [Intro to React](/spring2026/resources/web-ui-01-intro-to-react)  
2. [TypeScript & JavaScript Patterns](/spring2026/resources/web-ui-02-typescript-js-patterns)  
3. [Front-End Design with Mantine UI & Tailwind](/spring2026/resources/web-ui-03-mantine-tailwind)  
4. [Testing with Vitest](/spring2026/resources/web-ui-04-testing)  

## 2. Goal

1. Implement **Create / Read / Update / Delete** for Modules in `tma-starter-app/ui`.  
2. Handle **loading / error / empty / success** states.  
3. Keep your code TypeScript-safe and easy to adjust if your endpoint shape changes.  

## 3. Reference implementation (read-only)

Your UI does not need to match these files exactly, but the behavior should feel similar.

1. Create module page: 
    ```bash
    ui/src/pages/courses/CreateModulePage.tsx
    ```  
2. Module detail page (edit + delete): 
    ```bash
    ui/src/pages/courses/ModuleDetailPage.tsx
    ```  
3. Edit modal: : 
    ```bash
    ui/src/components/courses/EditModuleModal.tsx
    ```  
4. Module API helpers (Module section):
    ```bash
    ui/src/utils/api.ts
    ```  
5. Module types (Module section): 
    ```bash
    health-app/ui/src/types/api.ts
    ``` 

## 4. Team workflow requirement

1. Each teammate must open at least one PR that includes React/TypeScript changes.  
2. Your team merges one final PR to `main`.  
3. Before coding, create issues for your work:
   1. Make 1+ GitHub issues per teammate (one per “Person X checklist” in Section 5 works well).
   2. Put a short checklist + acceptance criteria in each issue (what should work when you’re done).
   3. Assign each issue to one person.
   4. Each PR should reference the issue(s) it addresses (and close them when merged).

## 5. Team plan: one checklist per person (use these as your issues)

### Person 1 (shared foundations)

- [ ] Add/confirm `Module`, `ModuleCreate`, and `ModuleUpdate` types in one shared place (see Section 7).  
- [ ] Add module API helper functions in one shared place (see Section 7).  
- [ ] Add the Module create + detail routes in `tma-starter-app/ui/src/App.tsx` (see Section 6.2).  
- [ ] Make sure other teammates can import the types + call the helpers without needing to know endpoint details.

### Person 2 (Course detail “Modules” section)

- [ ] Update the course detail page to show a Modules section (see Section 6.3).  
- [ ] Render the 4 UI states: loading, error, empty, success.  
- [ ] Make each module clickable to navigate to the module detail route.  
- [ ] Add a “Create Module” button/link that navigates to the create page route.

### Person 3 checklist (Create Module page)

- [ ] Build the Create Module page + form (see Section 6.4).  
- [ ] Wire submit to the create endpoint via the shared API helpers.  
- [ ] Show loading + errors on submit.  
- [ ] Navigate after success (back to course detail or to the new module’s detail).

### Person 4 checklist (Module detail + Edit)

- [ ] Build the Module detail page (see Section 6.2 + 6.5).  
- [ ] Fetch and display one module (loading/error/success states).  
- [ ] Add an Edit action (modal or page) and wire it to the update endpoint.  
- [ ] Refresh the displayed module data after a successful update.

### Person 5 checklist (optional: Delete + testing + polish)

- [ ] Add Delete on the Module detail page with confirmation (see Section 6.6).  
- [ ] Handle delete failures with a clear error message.  
- [ ] Add the minimal Vitest tests for a pure helper (see Section 8).  
- [ ] Do a quick UI polish pass (labels, spacing, empty states).

## 6. UI requirements (Module CRUD)

### 6.1. Where the UI should live <span class="badge">Everyone</span>

1. Add Module UI under the Course area (recommended), so Modules feel “part of Courses”.  
2. At minimum, support deep links to a module page via a route (not just a modal).  

### 6.2. Routes <span class="badge">Person 1</span>

Add routes in `tma-starter-app/ui/src/App.tsx` similar to how other dashboard routes are defined.

1. Create module route (example): `/dashboard/courses/:courseId/modules/new`  
2. Module detail route (example): `/dashboard/courses/:courseId/modules/:moduleId`  

If your backend does not use `courseId` in the module endpoints, you can still keep these routes for the UI.

### 6.3. List/Read <span class="badge">Person 2</span>

1. Update the course detail page (`tma-starter-app/ui/src/pages/courses/CourseDetailPage.tsx`) to show a Modules section.  
2. Show a “Create Module” action that navigates to the create page route.  
3. Each module should be clickable to navigate to its Module detail route.  
4. Handle UI states: loading, error, empty, success.  

### 6.4. Create <span class="badge">Person 3</span>

1. Build a Create Module page with a simple form:
   * `title` (required)  
   * `description` (optional)  
   * `color` (optional)  

2. On submit, call your backend “create module” endpoint and then navigate back to the course detail page (or the new module’s detail page).  

3. You can eventually refactor this page’s form into a reusable component (for example, to reuse it in a modal).


### 6.5. Update <span class="badge">Person 4</span>

1. On the Module detail page, provide an “Edit” action.  
2. Your edit UI can be a modal (like the reference) or a separate edit page.  
3. On submit, call your backend “update module” endpoint and refresh the displayed module data.  

### 6.6. Delete <span class="badge">Person 5 (optional)</span>

1. On the Module detail page, provide a “Delete” action.  
2. Ask for confirmation before deleting.  
3. If delete succeeds, navigate back to the course detail page (or a modules list).  
4. If delete fails (for example due to dependencies), show the error message.  

## 7. TypeScript + code organization requirements <span class="badge">Person 1</span> <span class="badge">Everyone</span>

1. Define `Module`, `ModuleCreate`, and `ModuleUpdate` types that match your backend.  
2. No `any` for module data or props.  
3. Put module API calls in one place (either):

   * Add a “Module API” section to `tma-starter-app/ui/src/utils/api.ts`, or  
   * Create a new file like `tma-starter-app/ui/src/utils/modulesApi.ts`).  

4. Use `useAuth()` to get `API_URL`, and include the bearer token in requests.  

## 8. Minimal testing requirement

1. Add 2 Vitest tests for one pure helper you wrote for this feature (examples: `buildModulesUrl`, `normalizeApiError`, `validateModuleTitle`).  
2. Keep tests small: test logic, not Mantine widgets.  

## 9. Submission

1. One team PR merged to `main`.  
2. Your final PR should close the issues your team created for this assignment (or link them clearly).  

